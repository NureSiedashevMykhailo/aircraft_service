# PostgreSQL Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=aircraft_monitoring
POSTGRES_PORT=5432

# API Configuration
API_PORT=3000
NODE_ENV=production

# Database URL (automatically constructed from above)
# DATABASE_URL=postgresql://postgres:postgres@postgres:5432/aircraft_monitoring?schema=public

